import { createServerClient } from "@supabase/ssr";
import { createClient } from "@supabase/supabase-js";
import { cookies } from "next/headers";
import { NextResponse } from "next/server";
async function clients(){const url=process.env.NEXT_PUBLIC_SUPABASE_URL;const anon=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY||process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY;const service=process.env.SUPABASE_SECRET_KEY||process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!anon||!service)return null;const store=await cookies();const auth=createServerClient(url,anon,{cookies:{getAll:()=>store.getAll(),setAll:v=>v.forEach(({name,value,options})=>store.set(name,value,options))}});const {data:{user}}=await auth.auth.getUser();if(!user)return null;return createClient(url,service,{auth:{autoRefreshToken:false,persistSession:false}})}
export async function GET(){const admin=await clients();if(!admin)return NextResponse.json({error:"ต้องเข้าสู่ระบบก่อน"},{status:401});const {data,error}=await admin.auth.admin.listUsers({page:1,perPage:100});if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({members:data.users.map(user=>({id:user.id,email:user.email,name:user.user_metadata?.full_name||user.email?.split("@")[0]||"สมาชิก",lastSignIn:user.last_sign_in_at}))});}
export async function POST(request:Request){const admin=await clients();if(!admin)return NextResponse.json({error:"ต้องเข้าสู่ระบบก่อน"},{status:401});const {email,name}=await request.json();if(typeof name!=="string"||name.trim().length<2||name.length>100)return NextResponse.json({error:"กรุณาใส่ชื่อ 2–100 ตัวอักษร"},{status:400});if(typeof email!=="string"||!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email))return NextResponse.json({error:"กรุณาใส่อีเมลให้ถูกต้อง"},{status:400});const {error}=await admin.auth.admin.inviteUserByEmail(email,{data:{full_name:name.trim()},redirectTo:`${new URL(request.url).origin}/auth/callback`});if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true,message:`ส่งคำเชิญให้ ${name.trim()} แล้ว`});}

